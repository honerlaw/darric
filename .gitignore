# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (may contain secrets)
.env
.env.*

# TypeScript build artifacts
tsconfig*.tsbuildinfo

# ML model files (whisper, etc.)
ggml-*.bin
*.gguf

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
**/.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local settings
.claude/

# minerva work-unit worktrees
.minerva/worktrees/

# Where CI checks out the pinned minerva tooling (check.yml, knowledge-reconcile.yml).
# Committing it would abort knowledge-reconcile.yml's post-merge checkout.
.minerva-tools/
